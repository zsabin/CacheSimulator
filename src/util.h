
#ifndef UTIL_H
#define UTIL_H

#include <stdlib.h>

typedef enum {FALSE, TRUE} Boolean;

/* Returns log base 2 of the specified value */
int log_2(int val);

/* Returns the result of 2 taken to the specified exponent */
int exp_2(int exp);

#endif /* UTIL_H */

